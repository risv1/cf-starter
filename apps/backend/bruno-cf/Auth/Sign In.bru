meta {
  name: Sign In
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/auth/sign-in
  body: json
  auth: inherit
}

body:json {
  {
    "email": "rishi@example.com",
    "password": "password"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
